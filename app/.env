# This deployment is same-origin: the Cloudflare Worker serves the static app
# AND the /api routes under one origin. In `npm run dev`, vite.config server.proxy
# forwards /ai /speech /me /health to the deployed Worker so AI + neural voice work.
VITE_SAME_ORIGIN=true
